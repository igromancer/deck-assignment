services:
  api:
    build:
      context: .
      target: api
    ports:
      - "8080:8080"
    environment:
      PORT: 8080
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: deck
    depends_on:
      - db
      - migrate
  db:
    image: postgres:18-alpine
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: deck
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U user -d deck",]
      interval: 3s
      timeout: 3s
      retries: 20
      start_period: 3s
    ports:
      - 5432:5432
  migrate:
    image: migrate/migrate:4
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: deck 
    depends_on:
      - db
    entrypoint: ["migrate"]
    command:
      - "-path"
      - "/db/migrations"
      - "-database"
      - "postgres://user:password@db:5432/deck?sslmode=disable"
      - "up"
    volumes:
      - ./db/migrations:/db/migrations
  adminer:
    image: adminer
    ports:
      - 9090:8080
